import Swipe from "./Swipe"
import { UserContext } from "./UserContext";
import { UseContext } from "react";

function Form({museums}) {
  // figure out routing
   
  fetch(`/museums/${id}`)
  return (
    <>
      <Swipe />
    </>
  )
}

export default Form
